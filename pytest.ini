[pytest]
testpaths = tests
addopts = -ra
markers =
    integration: marks tests exercising multiple components (deselect with -m "not integration")
    performance: marks long running performance benchmarks (deselect with -m "not performance")
    serialization: marks binary serialization specific tests
    algorithm: marks triangulation algorithm specific tests

